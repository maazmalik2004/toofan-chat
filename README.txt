* Running bot service *

Step 0: Create GOOGLE, MSITRAL and LANGCHAIN keys and add to .env inside the project root 

Step 1: create a virtual env(I am using python 3.12)

#python -m venv toofanenv
#source toofanenv/bin/activate
#pip3 install -r requirements.txt
python app.py


